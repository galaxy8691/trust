# Phase 0.2 — 标准库规范工程规格书

> 版本：v1.0 · 分支：phase0-spec · 对齐：`docs/Trust-设计文档.md §10` × `spec/trust-spec.md` × `docs/phases/0/TODO.md §0.2`  
> 里程碑代号：STDLIB-SPEC · 期限：Phase 0 第 3–4 周

---

## 0. 文件说明

本规格书定义 **Phase 0.2**（编写 Trust 标准库 API 规范）的工程范围、成功标准、里程碑和验收条件。

**产出物：** `spec/stdlib.md`

**一句话成功：** 每个 Trust 标准库模块的 API 签名完整到可以直接编写 `ferro_rt` 的 Rust 实现，无需回查设计文档或推断语义。

<dl>
  <dt>里程碑代号</dt><dd>0.2-STDLIB</dd>
  <dt>前置</dt><dd>Phase 0.1 — 语言规范冻结（spec/trust-spec.md 完成）</dd>
  <dt>下一里程碑</dt><dd>Phase 0.3 — 三方审计</dd>
  <dt>产出物</dt><dd>spec/stdlib.md</dd>
  <dt>对齐 TODO</dt><dd>docs/phases/0/TODO.md · 0.2 全部子项</dd>
  <dt>对齐设计文档</dt><dd>docs/Trust-设计文档.md §10</dd>
  <dt>对齐 constraints</dt><dd>docs/design-constraints.md §9.2（ferro_rt API 映射表）</dd>
</dl>

---

## 1. 成功标准

**MS-0.2-1（核心交付）：** `stdlib.md` 完成，覆盖 12 个模块（std::collections / sync / async / result / string / fs / rc / time / process / net / serde / crypto）。每个模块包含每个函数的完整签名（参数类型、返回类型、泛型约束）、一句话语义描述、对应 Rust 实现映射。

**MS-0.2-2（映射完备）：** Trust API → Rust 标准库/ferro_rt 映射关系表完整。每个 Trust API 函数有明确的 Rust 实现路径（例如 `std::fs::readToString` → `std::fs::read_to_string`）。

**MS-0.2-3（依赖图清晰）：** 模块间依赖图完整——明确哪些模块可以使用哪些模块，模块间的依赖不得引入循环引用。

---

## 2. 范围

### 2.1 纳入（12 个模块，按 v0.1/v0.2/v0.3 优先级）

| # | 模块 | 优先级 | 覆盖内容 | 对应设计文档 |
|---|------|--------|---------|------------|
| 1 | `std::collections` | v0.1 | `Vec<T>` `HashMap<K,V>` `HashSet<T>` `VecDeque<T>` 完整 API | §10 |
| 2 | `std::sync` | v0.1 | `Channel<T>` `Sender<T>` `Receiver<T>` `shared<T>` `Mutex<T>` `RwLock<T>` `AtomicI32` `spawn` | §10 |
| 3 | `std::async` | v0.1 | `join` `sleep` 异步 I/O 原语、Tokio 运行时绑定 | §10 §5.1.1 |
| 4 | `std::result` | v0.1 | `Option<T>`（`Some`/`None`/`isSome`/`isNone`/`unwrap`/`unwrapOr`/`map`/`andThen`/`expect`）`Result<T,E>`（`Ok`/`Err`/`isOk`/`isErr`/`unwrap`/`unwrapOr`/`map`/`andThen`/`expect`） | §10 §6.1 |
| 5 | `std::string` | v0.1 | UTF-8 字符串类型，方法：`split` `slice` `replace` `trim` `toUpperCase` `toLowerCase` `startsWith` `endsWith` `includes` `length` | §10 §11.9 |
| 6 | `std::fs` | v0.1 | `readToString` `writeString` `readDir` `metadata` `exists` `remove` — 全部返回 `Result` | §10 |
| 7 | `std::rc` | v0.1 | `Rc<T>` `Arc<T>` `Weak<T>` — `new` `clone` `downgrade` `upgrade` | §10 §4.5 |
| 8 | `std::time` | v0.1 | `Duration` `sleep` `now` `timestamp` | §10 |
| 9 | `std::process` | v0.2 | 子进程管理 `run` `spawn`、环境变量 `env.get` `env.set` | §10 |
| 10 | `std::net` | v0.2 | TCP/UDP 套接字 `TcpListener` `TcpStream`、HTTP 客户端 `http.get` `http.post`、TLS 支持 | §10 |
| 11 | `std::serde` | v0.2 | JSON/MessagePack 序列化 `fromStr` `toPrettyString`、基于 Rust serde 封装 | §10 |
| 12 | `std::crypto` | v0.3 | 哈希 `sha256` `blake3`、对称加密、非对称加密原语 | §10 |

### 2.2 排除

| 项目 | 归属 |
|------|------|
| ferro_rt 的 Rust 实现代码 | Phase 4（并发模型实现） |
| `std::net`/`std::serde` 的 bindgen 自动绑定 | Phase 6.3（bindgen 原型 v0.2.1） |
| 标准库的 Trust 源码实现（自举） | Phase 7.1 |
| `console.log` / `assert` 等调试/测试辅助函数 | Phase 6.1（测试框架） |
| `Dynamic` 枚举的内部实现 | Phase 1（编译器内置类型） |

---

## 3. 产出物规范

### 3.1 文件结构

```
spec/
├── trust-spec.md        # Phase 0.1 产出物
└── stdlib.md            # Phase 0.2 产出物（本规格书的产出物）

docs/phases/0/
├── TODO.md              # Phase 0 任务清单
├── 0.1/
│   └── 0.1-spec.md      # Phase 0.1 工程规格书
└── 0.2/
    └── 0.2-spec.md      # 本文件（工程规格书）
```

### 3.2 内容格式约定

`stdlib.md` 每个模块按以下结构组织：

1. **模块概述**（1-3 句，说明模块职责）
2. **API 签名表**（函数名、签名、语义描述、Rust 映射）
3. **类型定义**（如 `Option<T>` 的两个变体、`Result<T,E>` 的 `Ok`/`Err` 变体）
4. **设计决策**（记录关键 API 设计选择与否决方案）
5. **验收标准**（checklist，每条可编写测试验证）

API 签名格式示例：

```ts
// std::fs — 文件系统操作

/// 读取文件全部内容到字符串
function readToString(path: string): Result<string, FsError>

/// 将字符串写入文件（覆盖）
function writeString(path: string, content: string): Result<void, FsError>
```

### 3.3 各模块的编写任务

| 模块 | 最少函数数 | 最少类型定义数 | 设计决策 | 验收标准 |
|------|----------|-------------|---------|---------|
| `std::collections` | 20 | 4（Vec/HashMap/HashSet/VecDeque） | 1 段 | 8 |
| `std::sync` | 15 | 6（Channel/Sender/Receiver/Mutex/RwLock/Atomic） | 2 段 | 8 |
| `std::async` | 3 | 0 | 1 段（惰性 Future vs 热启动） | 4 |
| `std::result` | 10 | 2（Option/Result） | 2 段（! 仅限 Option、?? 语义） | 6 |
| `std::string` | 10 | 0 | 1 段 | 4 |
| `std::fs` | 6 | 1（FsError ADT） | 1 段 | 4 |
| `std::rc` | 6 | 3（Rc/Arc/Weak） | 1 段（Rc 非 Send 规则） | 4 |
| `std::time` | 4 | 1（Duration） | 0 | 2 |
| `std::process` | 4 | 1（ProcessResult） | 0 | 2 |
| `std::net` | 8 | 3（TcpListener/TcpStream/HttpClient） | 1 段（手写绑定→bindgen 迁移） | 4 |
| `std::serde` | 4 | 1（SerdeError） | 1 段（手写绑定→bindgen 迁移） | 2 |
| `std::crypto` | 4 | 1（HashResult） | 0 | 2 |

### 3.4 代码格式

- Trust API 签名使用 ` ```ts ` 代码块
- Rust 映射使用 ` ```rust ` 代码块  
- 模块依赖图使用 ASCII 图或 Mermaid

---

## 4. 阶段里程碑

| 里程碑 | 内容 | 完成标准 | 对应 TODO |
|--------|------|---------|----------|
| **MS-0.2-COL** | collections 模块冻结 | `std::collections` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-SYNC** | sync 模块冻结 | `std::sync` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-ASYNC** | async 模块冻结 | `std::async` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-RES** | result 模块冻结 | `std::result` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-STR** | string 模块冻结 | `std::string` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-FS** | fs 模块冻结 | `std::fs` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-RC** | rc 模块冻结 | `std::rc` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-TIME** | time 模块冻结 | `std::time` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-PROC** | process 模块冻结 | `std::process` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-NET** | net 模块冻结 | `std::net` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-SERDE** | serde 模块冻结 | `std::serde` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-CRYPTO** | crypto 模块冻结 | `std::crypto` API 全部验收标准勾选 | 0.2 ✅ |
| **MS-0.2-DEPS** | 依赖图冻结 | 模块间依赖图无循环引用 | 0.2 ✅ |
| **MS-0.2-MAP** | 映射表冻结 | Trust→Rust 映射表完成 | 0.2 ✅ |
| **MS-0.2-ALL** | **Phase 0.2 交付** | 以上全部 + TODO 0.2 全部 `[x]` | Phase 0.2 ✅ |

---

## 5. 追溯矩阵

| 模块 | 函数数 | 优先级 | 对齐设计文档 | 对齐 constraints | 可自动测试 |
|------|--------|--------|------------|----------------|----------|
| `std::collections` | 20 | v0.1 | §10 | §9.2 | ✅ |
| `std::sync` | 15 | v0.1 | §10 §5.2 §5.3 | §9.2 | ✅ |
| `std::async` | 3 | v0.1 | §10 §5.1.1 | §9.2 | ✅ |
| `std::result` | 10 | v0.1 | §10 §6.1 | — | ✅ |
| `std::string` | 10 | v0.1 | §10 §11.9 | — | ✅ |
| `std::fs` | 6 | v0.1 | §10 | — | ✅ |
| `std::rc` | 6 | v0.1 | §10 §4.5 | — | ✅ |
| `std::time` | 4 | v0.1 | §10 | — | ✅ |
| `std::process` | 4 | v0.2 | §10 | — | ⚠️ |
| `std::net` | 8 | v0.2 | §10 | — | ⚠️ |
| `std::serde` | 4 | v0.2 | §10 | — | ⚠️ |
| `std::crypto` | 4 | v0.3 | §10 | — | ⚠️ |

> v0.2/v0.3 模块的"可自动测试"标记为 ⚠️：API 签名可检查，但在 bindgen/ferro_rt 完成前无法运行端到端测试。

---

## 6. 模块依赖图

```
                    ┌─────────┐
                    │ result  │  (Option / Result — 所有其他模块的基础)
                    └────┬────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
    ┌────▼────┐    ┌─────▼──────┐   ┌───▼────┐
    │ string  │    │ collections│   │  fs    │
    └─────────┘    └────────────┘   └───┬────┘
         │                              │
    ┌────▼────┐                    ┌────▼────┐
    │  rc     │                    │  time   │
    └────┬────┘                    └─────────┘
         │
    ┌────▼────┐
    │  sync   │  (Channel / shared / spawn — 依赖 rc 的 Arc)
    └────┬────┘
         │
    ┌────▼────┐
    │  async  │  (join / sleep — 依赖 sync 的 spawn)
    └────┬────┘
         │
    ┌────▼────┐    ┌──────────┐    ┌──────────┐
    │  net    │    │  serde   │    │  process │
    └─────────┘    └──────────┘    └──────────┘
         │
    ┌────▼────┐
    │  crypto │
    └─────────┘
```

**规则：**
- `result` 是零依赖基础模块
- `sync` 依赖 `rc`（`shared` 使用 `Arc`，`Channel` 使用 `Sender`/`Receiver`）
- `async` 依赖 `sync`（`spawn async` 使用 Tokio）
- `net` `serde` `process` `crypto` 可依赖所有 v0.1 模块
- 禁止循环引用（例如 `fs` 不能依赖 `sync`）

---

## 7. 完成定义（DoD）

Phase 0.2 完成的 AND 条件：

- [ ] `spec/stdlib.md` 存在，包含 12 个模块的完整 API 签名
- [ ] 每个模块满足 §3.3 的最少函数数和最少类型定义数
- [ ] 每个模块至少包含 1 段设计决策文本（若 §3.3 要求 ≥1 段）
- [ ] Trust API → Rust 映射表完整（每个 Trust 函数有对应的 Rust 实现路径）
- [ ] 模块间依赖图无循环引用
- [ ] `std::result` 的 `Option` 和 `Result` 类型定义与 `spec/trust-spec.md` ERR 域一致
- [ ] `std::sync` 的 `Channel` `shared` `spawn` 签名与 `spec/trust-spec.md` CON 域一致
- [ ] `std::net` / `std::serde` 标注 "v0.2 手写绑定，v0.2.1 迁移至 bindgen"
- [ ] `docs/phases/0/TODO.md` 中 0.2 全部子项已勾选 `[x]`

---

> **下一步：** 按本规格书编写 `spec/stdlib.md`。  
> **审计：** Phase 0.3 对全部产出物进行三方交叉验证。
